<h1><%= @user.name %>のページ</h1>

<p>メールアドレス: <%= @user.email %></p>

<p>Image: <%= image_tag @user.image.url ,size:'300x200' if @user.image.present? && @user.image.url.present? %></p>

<%= link_to "index", posts_path %>
<%= link_to "edit", edit_user_path(@user.id) %>
<%#= edit_user_path(id)だとpostとuserどっちのパスかわからないから。　edit_user_path(:user_id)でもいける %>
